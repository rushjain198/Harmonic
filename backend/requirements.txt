# Harmonic website backend — song library (:8766) + multiplayer jam (:8502).
# Install with:  pip install -r requirements.txt
flask>=2.0
flask-cors>=3.0
librosa>=0.10
soundfile>=0.12
numpy>=1.20
pretty_midi>=0.2.9
fastapi>=0.100
uvicorn>=0.22

# The standalone desktop studio (ar_studio.py) additionally needs these — install
# only if you want that separate OpenCV window (NOT required for the website):
#   opencv-python
#   mediapipe
#   streamlit
#   simpleaudio
